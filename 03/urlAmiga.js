const texto = "Aprenda programar do zero com JavaScript";

const urlAmiga = texto

.toLowerCase (texto)
.replaceAll(" ", "-")


/*.trim () // 2. Remove espaços extras no início e no fim
.split (" ") // 3. Divide o texto em um array de palavras pelos espaços
.join ("-")  // 4. Junta as palavras usando hífen como separador*/

console.log(urlAmiga);



